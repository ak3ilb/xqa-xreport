name: Cleanup sample artifacts

# Deletes xreport-hourly-* artifacts older than 15 minutes via the Actions API.

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  cleanup:
    name: Delete aged artifacts
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Delete xreport-hourly artifacts older than 15m
        env:
          GH_TOKEN: ${{ github.token }}
          MAX_AGE_MINUTES: '15'
        run: |
          set -euo pipefail
          now=$(date -u +%s)
          cutoff=$((now - MAX_AGE_MINUTES * 60))
          deleted=0
          kept=0

          mapfile -t rows < <(gh api --paginate \
            "repos/${{ github.repository }}/actions/artifacts?per_page=100" \
            --jq '.artifacts[] | select(.name | startswith("xreport-hourly-")) | "\(.id)\t\(.name)\t\(.created_at)"' \
            || true)

          if [[ ${#rows[@]} -eq 0 || -z "${rows[0]:-}" ]]; then
            echo "No xreport-hourly artifacts found."
            echo "Deleted: 0" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          for row in "${rows[@]}"; do
            id="${row%%$'\t'*}"
            rest="${row#*$'\t'}"
            name="${rest%%$'\t'*}"
            created="${rest##*$'\t'}"
            created_epoch=$(date -u -d "${created}" +%s)

            if (( created_epoch <= cutoff )); then
              echo "DELETE id=${id} name=${name} created=${created}"
              gh api -X DELETE "repos/${{ github.repository }}/actions/artifacts/${id}"
              deleted=$((deleted + 1))
            else
              echo "KEEP   id=${id} name=${name} created=${created}"
              kept=$((kept + 1))
            fi
          done

          {
            echo "### Artifact cleanup"
            echo ""
            echo "- Max age: **${MAX_AGE_MINUTES} minutes**"
            echo "- Deleted: **${deleted}**"
            echo "- Kept: **${kept}**"
          } >> "$GITHUB_STEP_SUMMARY"
